# Flow Matching 目标架构

## 目标

针对随机初始位置的物体，根据 RGB observation condition 生成对应的抓取 joint action chunk。

历史 10 条数据曾混有完整自主 lift 与录制截断两种协议；当前 66 条目标数据已统一为完整的 policy 自主 lift。

lift 路径由全量审计自动裁决：完整录制占比大于 70% 走自主 lift 并剔除截断数据，小于 30% 走统一外部 lift，其余优先自主 lift 且仅在支持 prefix loss mask 后复用截断前缀。

当前目标训练走路径 B，使用 `a2d_parallel_1507_rgb_v1` 的 66 条完整 lift 数据，并将 action 明确定义为阶段 setpoint 而非连续控制轨迹；最新部署契约使用 `action_offset_steps=1`，确保当前观测只预测下一帧开始的动作。

## 项目进度

```text
数据管线    ✅ 封版（入库、审计、离线分段与 stats/split 哈希绑定均已验证）
训练基建    ✅ 封版（Frozen/Fine-tune 消融、encoder 监控、offset=1 数据契约与 checkpoint provenance 均已验证）
部署链路    ✅ 模型包就绪（bundle v2、强校验、动作哨兵、固定网格与四级验收协议均已就位）
A800 训练   ✅ 最新目标完成（Frozen ViT + next-action offset=1 已完成 30 epochs，并导出 best bundle）
仿真验证    ⏳ 待执行（推理包已通过真实 HDF5 离线测试，下一步接入推理 server 与 set-angle 执行链路）
```

三种 policy 共享双 RGB ViT、13 维 proprio 与 action Transformer 骨架、数据和训练预算，但各自独立训练权重并仅替换动作生成范式（CFM 预测速度场、DP 预测噪声、IMLE 直接由 latent 生成动作）；最终优劣仅由 ws-05 同协议 rollout 成功率裁决。

## 当前选定架构（2026-07-20）

```text
rgb_head[t] ───────┐
                   ├─ 冻结的 ViT（每相机 49 spatial tokens）─┐
rgb_right_hand[t] ─┘                                         │
                                                             ├─ 98 个视觉 token
proprio[t]（13维）─ Linear + LayerNorm ─ 1 个 proprio token ─┤
                                                             ↓
                                             condition 位置编码
                                                             ↓
                              Flow Matching Transformer（可训练）
                                                             ↓
                                    预测 16×13 Flow Velocity
                                                             ↓
                                           5 次 Flow ODE 积分
                                                             ↓
                         action[t+1:t+17]（16×13 绝对关节位置）
```

- 预训练 ViT backbone 已冻结，不参与反向更新；双相机共享该 backbone，并各保留 `7×7=49` 个空间 token。
- 仓库保留视觉 token 维度对齐 adapter 接口；当前 `vit_small_r26_s32_224` 输出维度与 `d_model=384` 相同，因此该 adapter 实际为 `Identity`，并不存在额外的可训练视觉线性层。冻结 ViT 后，真正参与视觉条件适配的是下游 condition 位置编码、proprio projection 和 Flow Matching Transformer。
- 训练时的主目标仍只有 straight-line CFM velocity MSE；`encoder_update_ratio`、`encoder_grad_param_ratio_mean`、`encoder_feature_std` 仅用于监控，不加入训练 loss。
- 推理时模型在同一个观测条件下迭代 5 次求解 Flow；这 5 次是生成一个 Action Chunk 的数值积分，不是仿真环境中的 5 次 rollout。
- 当前输出是绝对关节位置 setpoint：前 7 维对应 `arm2_pos`，后 6 维对应 `hand2_pos`。仿真端仍需负责 6→11 手部 mimic 展开、控制频率和每次实际执行的 chunk 步数。

## 当前架构

- 数据主线统一为“内嵌 RGB 原始 HDF5 → processed HDF5 → `A2DProcessedWindowDataset` → 训练”，external-RGB 模式已删除。
- 三路相机通过 `data.image_keys` 任意选择训练子集，不再固定全部启用。
- 当前 baseline 使用两路 RGB + 13 维 proprio，mask、qrcode 和 mesh/点云均留到 baseline 之后评估。
- 本地 `.venv` 已配置 `timm==0.9.16`，`vit_small_r26_s32_224` 权重下载、加载和 CPU 前向均已通过。
- 本地 CPU smoke 使用 CNN、两路 64×64 RGB、batch size 2 和 2 epochs 验证完整训练链路。
- 单卡 A800 已用 batch size 32、fp32 完成 66 条数据的 Frozen ViT、next-action offset=1、30-epoch 目标训练并产出 best bundle；RTX 4090/仿真设备仅作为后续 rollout 执行端。
- timm 默认保留每路 7×7=49 个 spatial tokens，两路相机共生成 98 个 condition tokens。
- 模型主线为 `obs tokens → action tokens + time → self-attn + cross-attn → velocity`。
- 当前主模型训练目标为 straight-line CFM velocity MSE。
- action 固定为实际执行的 `arm2_pos(7)+hand2_pos(6)=13` 维，稀疏的 `*_pos_target` 不参与训练。
- 时间对齐固定为 `obs[t] → action[t+1:t+17]`；`chunk[0]` 是下一帧而不是当前帧，旧 offset=0 checkpoint 只保留作历史对照，不得用于该推理契约。
- state/action 使用 train episodes 的逐维 min/max 归一化，stats 会记录并校验 action semantics。
- 归一化同时使用 `range_eps=1e-4` 保护近常量维度，并将完整 normalizer 内嵌进 checkpoint。
- proprio 使用归一化后的 13 维当前 joint state，并作为一个独立 condition token 与视觉 tokens 拼接。
- Dataset 在建索引时强制校验 13 维 action、字段布局、所选相机和帧数一致性。
- Dataset 默认用 episode 内容哈希拒绝重复样本，单 episode CPU smoke 仅通过显式开关临时放行副本。
- `*_pos_target` 的稀疏写入只保留为采集诊断信息，不再阻断 processed 数据、stats 或训练。
- timm 输入按 backbone `pretrained_cfg` 的 mean/std 归一化，当前模型的 mean/std 均为 0.5。
- 首次正式训练启用 `0.2/0.4/0.2/0.05` ColorJitter，各相机独立采样且同一相机的历史帧共享参数。
- optimizer 支持 `backbone_lr_multiplier`；消融组用 0.1× 学习率微调 ViT，当前选定组将 multiplier 设为 0 并把 backbone 从 AdamW 中排除。
- optimizer 使用比例式 warmup + cosine scheduler，checkpoint 同时保存训练状态与完整推理契约。
- 首 0→2 帧 EEF 位移约 `0.299m+0.092m`，说明 arm 数据是低频阶段关键点而非完整 approach 轨迹，hand 才是高频逐帧控制。
- 66 条完整 lift 数据中 arm-keyframe 窗口约占 7%，train 使用 2 倍过采样后约占 13%，keyframe/continuous loss 健康比值约为 1.13，val 保持原始分布。
- 动静态分段与过采样只依据 executed `arm2_pos/hand2_pos` 速度阈值，不再使用 `*_pos_target` 或硬编码帧号。
- 窗口按 executed action 三分为 keyframe、continuous、static，训练仅对包含 arm 大跳变的 keyframe 窗口做过采样并分别记录三类 loss。
- 训练与验证分别记录 static/continuous/keyframe loss，用于监控保持段捷径与关键帧学习情况。
- stats 输出每维 unique 数量和范围告警，避免近常量关节的数值噪声被 min/max 放大。
- processed HDF5 已物化带版本和阈值的 executed-action 分段标签，Dataset 不再在线重算分段。
- stats 已绑定排序后的 train episode 内容哈希列表及摘要，split 不一致会硬失败且 checkpoint 同步保存摘要。
- 版本化入库命令已串联审计门禁、quarantine、预处理、episode manifest 与 stats，新增数据必须使用新的 dataset version。
- eval bundle 已记录 stats/分段/训练环境 provenance，4090 rollout 启用动作范围哨兵并按观测对齐、GT 回放、训练位姿闭环、182 网格四级验收。
- 正式跨机交付禁止只传裸 ckpt，统一传输自包含的 `bundle TGZ + reference_input.npz + reference_output.json + requirements lock + SHA256SUMS` 目录，每一跳落地必须通过 `sha256sum -c` 与 Level 0 同 seed `atol` 对账后才允许进入 rollout。
- 数据策略已从“等待 350GB 存量搬运”收口为“新管线持续增量产出 + 版本化入库 + 覆盖缺口定向补采”，当前约 20GB/66 条数据已满足本项目训练目标。
- 66 条数据的物体位姿必须按 robot-root local 坐标统计：`X=0.592~0.887m`、`Y=-0.318~0.050m`，最近邻距离 P50=`1.62cm`、P95=`5.38cm`，5×5 网格覆盖 19/25，其余 6 格作为 sparse 组与定向补采清单。
- 近邻物体位姿对应的 grasp arm 解差异较大，说明同一局部位姿附近存在多种合法抓法并适合用 flow matching 建模。
- 固定 rollout 考卷将 100 次密集区 XY 插值作为主指标，并将 50 次稀疏边界与 32 次 yaw sweep 分组独立报告。
- best checkpoint 按 `val_loss` 选择，最终模型效果以 rollout 成功率为准。
- CPU smoke 已验证数据读取、前后向、loss、验证、metrics 和 `best.ckpt` 全链路。
- 单 episode 复读已通过，训练、采样、反归一化和 executed-pos 分段诊断的 CPU 验收正式关闭。
- 历史十条内嵌 RGB episode 已完成结构审计，其中六条完整 lift 数据仅用于早期小规模 baseline。

## A800 首轮六条历史基线（2026-07-13）

- A800 环境为 `torch==2.4.1+cu121`、`timm==0.9.16`，预训练权重已在 `HF_HUB_OFFLINE=1` 下完成加载和前向验证。
- A800 代码由 GitHub Private 仓库的只读 Deploy Key 拉取，首轮 bundle 对应 `git_sha=c03a1de7912915a5cf54e9018b34a05a94ad1663`，训练工作树干净。
- 六条 processed episode 已完成跨机文件 SHA-256、episode 内容哈希、stats digest 和固定 split 成员重算校验；train/val 分别为 5/1 条 episode，窗口数分别为 732/130。
- 单 batch 32、固定 flow noise/time 的 50-step 过拟合由 `1.7523` 降至 `0.07258`，loss ratio 为 `4.14%`。
- 2-epoch timm smoke 已验证完整训练、CFS checkpoint、eval bundle 和断点恢复；CUDA checkpoint 恢复时的 RNG state 设备错误已修复并由 `RESUME_OK` 留下 lineage。
- 正式首轮为 batch 32、fp32、20 epochs、460 optimizer steps，总耗时 `235.869s`；最终 train flow loss 为 `0.10276`，train sample-action MSE 为 `0.00812`，val loss 为 `0.31233`。
- 最终 continuous/keyframe loss 为 `0.07316/0.19104`；keyframe 约为 continuous 的 2.6 倍，该数值仅作为 6 条数据时代的历史参照。
- 首轮 best 与 final 均为 epoch 19 / step 460；训练目录为 `/share_data/zhangyurui/flow-matching-test-a2d-v2/runs/a800_6episode_full_20260713_205141/`。
- `best.ckpt` SHA-256 为 `9a15e2db1109fe269908af8c777705e4b251fe55cb8f50b11e8432fc291f3eb1`；step 460 tgz 已通过 gzip 完整性检查，bundle 内的所有文件均由 `manifest.json` 记录 SHA-256。
- step 460 bundle 是第一个具备完整 `git_sha + dataset_manifest_sha256 + split_manifest_sha256 + stats_digest + segmentation_version` 的可部署基线产物。
- W&B 采用分级策略：分钟级调试/小数据 run 允许 A800 `online` 实时上传且必须在异常时降级为 no-op，长训保留 `offline 落盘 → psibot 同步验收`的安全链路；A800 的 `HOME=$WORK/home` 位于共享 CFS，仅保存非敏感缓存并设为 `700`，online 密钥只允许由 TI-ONE Secret 注入或保存在容器本地 `/root/.secrets/wandb_api_key`（`600`，重建后重新注入）。
- 6 条历史数据的 100-epoch online 基线 `vfj3l2u5` 已以终点指标偏差 ≤1.2% 完成复现，仅作为训练基建参照，不再代表当前目标模型。
- CFM、RS-IMLE、Diffusion 已完成统一接口、bundle/rollout 兼容与 A800 过拟合验证，正式三方比较冻结按 `docs/POLICY_COMPARISON_PROTOCOL.md` 执行：同数据/网络/step/seed，以 rollout 成功率为主、sample MSE 与推理延迟为辅，禁止横向比较三种训练 loss。

## A800 66 条目标训练（2026-07-15）

- 数据版本为 `a2d_parallel_1507_rgb_v1`，66/66 条 episode 经 `object_pose + lift phase + EEF 尾段位移` 三重证据确认完整，train/val 为 60/6 条。
- batch 32、fp32 训练 30 epochs / 7380 steps，best 为 epoch 18 / step 4674，`val_loss=0.06124`、`val_sample_action_mse=0.01386`；final `val_loss=0.06427`，训练目标已健康收敛。
- best bundle 对应 stats digest `f34e7703be9c8374574d740524491740a12bd9940e1ac80d4457f7b175647f14`，TGZ SHA-256 为 `ebe1d1eb52de01e25d8c581c357c5bd71c5f5ccb30717c7a065812579335ecd8`。
- 6 条 val 关键点可视化显示 grasp 手部平均 MSE 为 `0.00141 rad²`，但其中 3 条 arm-pregrasp 误差约为 `0.078~0.192 rad²`，必须在 Level 3 中重点裁决。
- Level 0 参考包已生成于 A800 `rollout_artifacts/level0_prep_66ep_step4674/`：`episode_env_000_000008_success.hdf5 / frame 0 / seed 20260715 / TF32 off`，包含模型输入 NPZ、16×13 输出 JSON、bundle TGZ、A800 lock 与 `SHA256SUMS`，四项校验全绿。

## Frozen ViT 与下一帧动作版本（2026-07-20）

- 同数据、split、seed、batch、epoch 和调度器的 offset=0 消融中，Frozen ViT 的 best val flow loss 为 `0.041467`，Fine-tune 0.1× 为 `0.061160`；微调组训练 loss 更低但 train→val gap 更大，因此当前 66-episode 规模选择 Frozen ViT。
- 上述消融 checkpoint 仍是旧 `offset=0` 时间契约，只用于证明冻结策略，不作为最终推理模型。
- `a78fa8f` 将数据窗口统一改为 `action_offset_steps=1`，并对完整窗口过滤、分段标签、checkpoint provenance、resume 兼容与 bundle 配置同步升级。
- 最新正式 run 为 `cfm_66ep_vit_frozen_next_action_seed42`：30 epochs / 7320 steps，best `val_loss=0.051097`（epoch 21），best `val_sample_action_mse=0.013971`（epoch 27），final `train_loss=0.031383`、final `val_loss=0.051328`。
- W&B run：`arzpl1mx`；best checkpoint SHA-256：`a46ac17dc019968113b7899008084ac96e93193b37c24e944fcbb44ba1781344`。
- 最新 eval bundle 为 `cfm_frozen_next_action_offset1_seed42_best.tgz`，归档 SHA-256 为 `6225396967997d96ff4911e186612a7535091f9395d4f424f38481c56afd2175`，manifest 锚定训练代码 `a78fa8f89e504e18100c424bdf1216540be8dec7`。
- 本地使用 `episode_000002_success.hdf5` 的 frame 0 完成 CPU 离线推理：输出 `(16,13)` float32、全部有限、动作范围哨兵通过；与演示帧 1～16 的单 chunk MSE 为 `0.0276808`。该结果只证明推理链路与时间对齐可运行，最终效果仍由闭环 rollout 成功率裁决。

## 4090 rollout 验收主线

```text
Level 0  bundle 落盘与强校验
         └─ 传输 SHA-256、bundle 文件哈希、stats/split/segmentation provenance、
            policy_wrapper 加载、torch/timm/CUDA 环境差异记录

Level 1  观测对齐
         └─ Isaac 渲染帧 vs 训练帧：SSIM + 重投影误差，阈值随场景版本存档

Level 2  GT 回放与物理标定
         └─ executed 轨迹回放、留出 episode 验证，并将 run_metadata.json 中
            尚为 null 的质量、摩擦、solver 等参数填成实测值

Level 3  训练位姿闭环
         └─ execute_horizon=16，在训练物体位姿上验证完整抓取与 lift 闭环

Level 4  泛化半径地图
         └─ 固定 182 网格，并对 execute_horizon=16/8/4 做对照
```

- Level 0 可以直接执行；4090 本地推理环境尚未与 A800 lock 对账，版本差异必须记录，不能把校验警告习惯性忽略。
- Level 1～3 的真实验收依赖场景资产、相机标定和物理参数；资产缺失时必须明确报错停止，禁止用临时假参数产出 rollout 结论。
- `hand2_pos` 已确认与 `hand2_all_pos` 前 6 个主动关节逐帧一致，`6→11` 规则固定为 URDF 的 5 条 mimic 声明；Level 0.5 必须通过无接触单位输入逐条验证 USD 比例，导入器丢失 mimic 时按 URDF 真值回退显式耦合层，禁止从接触轨迹回归规则。
- Level 3 以 6 条未见 val 的 robot-local 物体位姿为考卷，每位姿采样 5 次，并对 3 条 arm-pregrasp 弱位姿单独归因；Level 4 的 182 网格为非阻塞扩展项。

## 输入输出契约

```yaml
model_schema:
  obs_keys:
    - rgb_head
    - rgb_right_hand
  action_keys:
    - arm2_pos  # 7
    - hand2_pos # 6
  action_dim: 13
  action_horizon: 16
  action_offset_steps: 1
  action_semantics: executed_joint_position
  temporal_contract: "obs[t] -> action[t+1:t+17]"
```

## 后续事项

- 后续数据仅按新管线增量入库与 robot-local 稀疏格定向补采，不再将 350GB 存量搬运或再训练列为当前项目交付前提。
- EMA 与按 optimizer step 验证已明确推迟到第二次长训前实现，不阻塞首次正式训练。
- 当前 HDF5 没有 joint/camera timestamp，因此全量数据仍需在首尾过渡段保留视觉一致性抽检。
- 当前仓库已有 rollout harness、固定网格和分级判据；当前剩余主任务是把最新 offset=1 bundle 接入推理 server，将每个 13 维绝对关节 setpoint 转成仿真 `set angle` 信号并完成 Level 0→3。
- A800 本轮训练已完成；在最新模型完成闭环 rollout 前，不再以扩大数据规模、重新微调 ViT 或调优 CFS 吞吐作为主任务。
- 场景资产与相机位姿/内参必须由采数资产和标定文件生成，不允许手调后作为正式验收依据。
- `6→11` 规则已由 URDF mimic 声明确定，当前仅剩 Isaac USD 导入结果的无接触比例验证；通过前手部 rollout 不得标记为可用。
- rollout 成功判据固定为抓取位姿到位精度、hand 闭合成功和 lift 后物体不掉三部分。
- 当前 66 条数据已统一为 policy 自主 lift；后续增量数据仍禁止混合自主 lift 与外部脚本 lift 标签训练。

## 最小 CPU smoke

```bash
cd /home/psibot/Downloads/flow-matching-test
source .venv/bin/activate
python -m flow_matching_test.train --config configs/cpu_smoke.yaml data.data_dir=/path/to/a2d_processed training.output_dir=/tmp/a2d_cpu_smoke
```
